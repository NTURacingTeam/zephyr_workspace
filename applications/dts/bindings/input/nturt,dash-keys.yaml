description: NTU Racing Team dashboard buttons

compatible: "nturt,dash-keys"

properties:
  index:
    required: true
    type: int
    description: |
      CANopen object dictionary entry index of the buttons.
  zephyr,code:
    required: true
    type: array
    description: |
      Key code to emit for each button.
